{% extends "MeupfPlateformBundle::layout.html.twig" %}

{# code ne marche pas  ca redirecte sur l'index todo #}
{% block title %}

    Supprimer un evenement - {{ parent() }}

{% endblock %}


{% block meupfplateform_body %}


<h2>Supprimer un evenement</h2>


<p>

    Etes-vous certain de vouloir supprimer l'évenement "{{ event.title }}" ?

</p>


{# On met l'id de l'annonce dans la route de l'action du formulaire #}

<form action="{{ path('meupf_service_delete', {'id': event.id}) }}" method="post">

    <a href="{{ path('meupf_service_view', {'id': event.id}) }}" class="btn btn-default">

        <i class="glyphicon glyphicon-chevron-left"></i>

        Retour à la page evenement

    </a>

    {# Ici j'ai écrit le bouton de soumission à la main #}

    <input type="submit" value="Supprimer" class="btn btn-danger" />

    {# Ceci va générer le champ CSRF #}

    {{ form_rest(form) }}

</form>

